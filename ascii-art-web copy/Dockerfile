# Используем образ с Go
FROM golang:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы Go-приложения внутрь контейнера
COPY . .

# Собираем приложение
RUN go build -o main .

# Ваше приложение будет слушать порт 8080
EXPOSE 8080

# Команда для запуска вашего приложения
CMD ["./main"]
